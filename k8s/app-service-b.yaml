apiVersion: v1
kind: Service
metadata:
  name: lb-b
spec:
  type: LoadBalancer
  externalTrafficPolicy: Cluster
  selector:
    app: app-b
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
